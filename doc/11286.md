---
title: "11286 - Nginx Snippet"
---

# 11286 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "unruly-underneath.com"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name lasting-bracelet.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:4908;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [8285 - License Header](./8285)
- [8143 - Keyboard Shortcuts](./8143)
- [10334 - API Gateway](./10334)
