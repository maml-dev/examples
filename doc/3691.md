---
title: "3691 - Nginx Snippet"
---

# 3691 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "wrong-promise.info"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name ideal-clamp.biz;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:6073;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [3426 - Search Config](./3426)
- [2970 - App Manifest](./2970)
- [3569 - Webhook Config](./3569)
