---
title: "10690 - Nginx Snippet"
---

# 10690 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "amazing-babushka.net"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name deserted-cassava.org;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:7396;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [10384 - Shell Scripts](./10384)
- [4418 - Cron Schedule](./4418)
- [2447 - Server Inventory](./2447)
