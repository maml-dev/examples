---
title: "7796 - Nginx Snippet"
---

# 7796 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "any-analogy.com"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name devoted-mainstream.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:8561;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [2126 - API Gateway](./2126)
- [572 - Docker Compose](./572)
- [3214 - Redis Config](./3214)
