---
title: "8900 - Nginx Snippet"
---

# 8900 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "irresponsible-birdcage.info"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name understated-comparison.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:3240;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [1592 - Nginx Snippet](./1592)
- [5714 - Proxy Config](./5714)
- [598 - Shell Scripts](./598)
