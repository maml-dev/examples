---
title: "590 - Nginx Snippet"
---

# 590 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "subdued-doubter.org"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name vengeful-brush.net;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:7987;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [319 - Build Config](./319)
- [012 - S3 Config](./012)
- [443 - Cron Jobs](./443)
