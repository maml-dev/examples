---
title: "7793 - Nginx Snippet"
---

# 7793 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "quick-witted-resolve.info"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name productive-expansion.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:5110;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [5285 - API Mock](./5285)
- [2656 - Font Stack](./2656)
- [3203 - App Settings](./3203)
