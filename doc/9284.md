---
title: "9284 - Nginx Snippet"
---

# 9284 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "failing-discourse.biz"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name grubby-testing.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:4074;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [3519 - Package Config](./3519)
- [8475 - Docker Compose](./8475)
- [5989 - Log Entries](./5989)
