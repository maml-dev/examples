---
title: "6483 - Makefile Targets"
---

# 6483 - Makefile Targets

Build targets with embedded shell commands. This is an example of a [MAML](https://maml.dev) document.

```maml
# Build targets
{
  project: "hamburger"
  default: "build"
  targets: {
    # Build the application
    build: {
      deps: [
        "clean"
        "lint"
      ]
      script: """
echo "Building..."
go build -o bin/app ./cmd/app
echo "Build complete"
"""
    }

    # Run tests with coverage
    test: {
      script: """
echo "Running tests..."
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
"""
    }

    clean: {
      script: """
rm -rf bin/ dist/ coverage.out coverage.html
echo "Cleaned"
"""
    }

    # Docker build and push
    docker: {
      deps: [
        "build"
      ]
      script: """
docker build -t fund:latest .
docker push apparatus:latest
"""
    }
  }
}
```

## See Also

- [3192 - API Mock](./3192)
- [5068 - Test Suite](./5068)
- [3471 - Migration Plan](./3471)
