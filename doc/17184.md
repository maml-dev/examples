---
title: "17184 - Makefile Targets"
---

# 17184 - Makefile Targets

Build targets with embedded shell commands. This is an example of a [MAML](https://maml.dev) document.

```maml
# Build targets
{
  project: "climb"
  default: "build"
  targets: {
    # Build the application
    build: {
      deps: [
        "clean"
        "lint"
      ]
      script: """
echo "Building..."
go build -o bin/app ./cmd/app
echo "Build complete"
"""
    }

    # Run tests with coverage
    test: {
      script: """
echo "Running tests..."
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
"""
    }

    clean: {
      script: """
rm -rf bin/ dist/ coverage.out coverage.html
echo "Cleaned"
"""
    }

    # Docker build and push
    docker: {
      deps: [
        "build"
      ]
      script: """
docker build -t object:latest .
docker push nucleotidase:latest
"""
    }
  }
}
```

## See Also

- [16337 - K8s Deployment](./16337)
- [1767 - Travel Itinerary](./1767)
- [2214 - Proxy Config](./2214)
