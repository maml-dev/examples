---
title: "2585 - Nginx Snippet"
---

# 2585 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "untimely-perfection.biz"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name aged-oil.name;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:4899;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [1451 - Daily Agenda](./1451)
- [606 - Redis Config](./606)
- [2408 - Linter Config](./2408)
