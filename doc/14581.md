---
title: "14581 - Makefile Targets"
---

# 14581 - Makefile Targets

Build targets with embedded shell commands. This is an example of a [MAML](https://maml.dev) document.

```maml
# Build targets
{
  project: "adaptation"
  default: "build"
  targets: {
    # Build the application
    build: {
      deps: [
        "clean"
        "lint"
      ]
      script: """
echo "Building..."
go build -o bin/app ./cmd/app
echo "Build complete"
"""
    }

    # Run tests with coverage
    test: {
      script: """
echo "Running tests..."
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
"""
    }

    clean: {
      script: """
rm -rf bin/ dist/ coverage.out coverage.html
echo "Cleaned"
"""
    }

    # Docker build and push
    docker: {
      deps: [
        "build"
      ]
      script: """
docker build -t sonnet:latest .
docker push gazebo:latest
"""
    }
  }
}
```

## See Also

- [13282 - Error Messages](./13282)
- [7619 - Git Config](./7619)
- [8482 - Changelog](./8482)
