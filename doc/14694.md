---
title: "14694 - Nginx Snippet"
---

# 14694 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "jam-packed-numeric.name"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name slow-numeric.org;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:7317;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [8494 - Log Entries](./8494)
- [7201 - Git Config](./7201)
- [12160 - API Routes](./12160)
