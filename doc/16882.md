---
title: "16882 - Nginx Snippet"
---

# 16882 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "shadowy-fax.name"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name leading-guidance.info;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:3044;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [1219 - Package Config](./1219)
- [16848 - Dependencies](./16848)
- [133 - Terraform Resource](./133)
