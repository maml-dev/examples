---
title: "1299 - Nginx Snippet"
---

# 1299 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "boiling-reservation.name"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name quarrelsome-pneumonia.net;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:8121;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [1231 - API Gateway](./1231)
- [1280 - Recipe](./1280)
- [850 - API Routes](./850)
