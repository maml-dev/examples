---
title: "5782 - Nginx Snippet"
---

# 5782 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "pushy-unique.net"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name defenseless-pliers.name;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:5865;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [1662 - Recipe](./1662)
- [2737 - Auth Config](./2737)
- [3678 - Migration Plan](./3678)
