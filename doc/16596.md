---
title: "16596 - Nginx Snippet"
---

# 16596 - Nginx Snippet

Nginx server block as raw string configuration. This is an example of a [MAML](https://maml.dev) document.

```maml
# Nginx configuration
{
  name: "major-decision.name"
  # Server block
  config: """
server {
    listen 443 ssl http2;
    server_name vast-allegation.com;

    ssl_certificate /etc/ssl/certs/cert.pem;
    ssl_certificate_key /etc/ssl/private/key.pem;

    location / {
        proxy_pass http://localhost:6944;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        root /var/www/html;
        expires 30d;
    }
}
"""

  enabled: true
  reload_command: "nginx -t && systemctl reload nginx"
}
```

## See Also

- [3839 - Backup Strategy](./3839)
- [16050 - Keyboard Shortcuts](./16050)
- [1984 - Regex Patterns](./1984)
