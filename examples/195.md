---
title: "195 - Makefile Targets"
---

# 195 - Makefile Targets

Build targets with embedded shell commands. This is an example of a [MAML](https://maml.dev) document.

```maml
# Build targets
{
  project: "bandwidth"
  default: "build"
  targets: {
    # Build the application
    build: {
      deps: [
        "clean"
        "lint"
      ]
      script: """
echo "Building..."
go build -o bin/app ./cmd/app
echo "Build complete"
"""
    }

    # Run tests with coverage
    test: {
      script: """
echo "Running tests..."
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
"""
    }

    clean: {
      script: """
rm -rf bin/ dist/ coverage.out coverage.html
echo "Cleaned"
"""
    }

    # Docker build and push
    docker: {
      deps: [
        "build"
      ]
      script: """
docker build -t giant:latest .
docker push halt:latest
"""
    }
  }
}
```

## See Also

- [031 - Monitoring Config](./031)
- [064 - App Manifest](./064)
- [157 - Bookmarks](./157)
